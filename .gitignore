 @@ -0,0 +1,46 @@
# Python Byte-code
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
.pytest_cache/

# Virtual Environment
venv/
.venv/  # Common alternative for venv if you create it this way

# Editor/IDE Files
.vscode/        # VS Code-specific settings (can be useful to share, but often individual)
.idea/          # JetBrains IDEs (PyCharm)
*.swp           # Vim swap files
*~              # Old backup files

# Operating System Files
.DS_Store       # macOS
Thumbs.db       # Windows

# Data / Generated Files (IMPORTANT for Clarity project)
data/*.csv      # Ignore all CSVs in the data folder initially, commit 'clean_job_data.csv' specifically IF it's small and stable.
data/*.json     # If you store any JSON results
data/*.xlsx     # If you export to Excel
*.log           # Log files
*.sqlite3       # SQLite database files

# Environment Variables / Credentials (CRUCIAL for security)
.env            # Environment variables file (e.g., API keys, database credentials)
*.env           # Generic env files
api_keys.py     # Example, if you store keys directly in a Python file (HIGHLY discouraged, use .env)
credentials.json # Example

# Test/Coverage files
.coverage
htmlcov/

# Python Build artifacts
dist/
build/
*.egg

# Jupyter / IPython Notebooks (if you use them for prototyping)
.ipynb_checkpoints/
